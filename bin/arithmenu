#! /bin/env bash

while inp=$(echo -e "$oup" | dmenu -i -h 12 -uh 1 -p "=" -o 0.7 -fn "Terminus-8:normal" -nb \#48483e -nf \#76715e -sb \#48483e -sf \#cfd0c2 -uc \#a7e22e)
do
    if oup2=$(calc -pd "$inp"); then
        echo -n "$oup2" | xsel -i
        oup="$oup2\n$oup"
    fi
done
