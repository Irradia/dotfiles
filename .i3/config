# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

# Variables {{{
set $mod Mod4
# }}}
# Misc {{{
font pango:Terminus 8

new_window pixel 3
new_float normal 0

gaps inner 5
gaps outer 15

focus_follows_mouse yes
mouse_warping none

force_focus_wrapping no
workspace_auto_back_and_forth yes
smart_borders on
popup_during_fullscreen smart
force_display_urgency_hint 750 ms
# }}}
# Non-wm binds {{{
bindsym $mod+Return exec urxvt
bindsym $mod+Shift+Return exec urxvt -name float
bindsym $mod+Escape exec ~/bin/suppressed-lock

# bindsym $mod+d exec dmenu_run -i -h 12 -uh 1 -p "$" -fn "Terminus-8:normal" -nb \#48483e -nf \#76715e -sb \#48483e -sf \#cfd0c2 -uc \#a7e22e
bindsym $mod+d exec "rofi  -modi window,run,ssh -show run"
bindsym $mod+Shift+d exec "rofi  -modi window,run,ssh -show window"

bindsym $mod+bracketright exec ~/bin/mathmen
# bindsym $mog+bracketleft TODO: spell check script

# bindsym $mod+q exec ~/tmp/git/clipmenu/clipmenu

bindsym $mod+shift+z exec killw $(pfw)
bindsym $mod+x kill

# bindsym $mod+o exec i3-msg open

bindsym --release Print exec --no-startup-id scrot '%Y-%m-%d_%H:%M:%S_$wx$h.png' -e 'mv $f ~/tmp/scr' ; exec notify-send -u low 'screenshot' 'root dumped'
bindsym --release Shift+Print exec --no-startup-id scrot -s '%Y-%m-%d_%H:%M:%S_$wx$h.png' -e 'mv $f ~/tmp/scr' && exec notify-send -u low 'screenshot' 'selection dumped'

bindsym XF86AudioRaiseVolume exec amixer set Master 5%+
bindsym XF86AudioLowerVolume exec amixer set Master 5%-
bindsym XF86AudioMute exec amixer set Master toggle

bindsym $mod+Shift+equal exec mpc toggle

# bindsym $mod+Shift+c reload
bindsym $mod+Shift+r restart
# }}}
# Containers {{{
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

bindsym $mod+c split h
bindsym $mod+v split v

bindsym $mod+w layout stacking
bindsym $mod+e layout tabbed
bindsym $mod+f layout toggle split

bindsym $mod+u fullscreen toggle
bindsym $mod+Shift+u fullscreen toggle global

bindsym $mod+i sticky toggle

bindsym $mod+o move absolute position center

floating_modifier $mod
bindsym $mod+y floating toggle
bindsym $mod+grave focus mode_toggle
bindsym --whole-window $mod+button2 floating toggle

bindsym $mod+a focus parent
bindsym $mod+s focus child

bindsym $mod+t border normal 0
bindsym $mod+g border pixel 3
# }}}
# Workspaces {{{
bindsym $mod+1 workspace 1:1
bindsym $mod+2 workspace 2:2
bindsym $mod+3 workspace 3:3
bindsym $mod+4 workspace 4:4
bindsym $mod+5 workspace 5:5
bindsym $mod+6 workspace 6:6
bindsym $mod+7 workspace 7:7
bindsym $mod+8 workspace 8:8
bindsym $mod+9 workspace 9:9
bindsym $mod+0 workspace 10:0

bindsym $mod+Shift+1 move container to workspace 1:1
bindsym $mod+Shift+2 move container to workspace 2:2
bindsym $mod+Shift+3 move container to workspace 3:3
bindsym $mod+Shift+4 move container to workspace 4:4
bindsym $mod+Shift+5 move container to workspace 5:5
bindsym $mod+Shift+6 move container to workspace 6:6
bindsym $mod+Shift+7 move container to workspace 7:7
bindsym $mod+Shift+8 move container to workspace 8:8
bindsym $mod+Shift+9 move container to workspace 9:9
bindsym $mod+Shift+0 move container to workspace 10:0

bindsym $mod+minus move scratchpad
bindsym $mod+equal scratchpad show

bindsym $mod+Tab workspace next
bindsym $mod+Shift+Tab workspace prev
# }}}
# Window specifics {{{
for_window [instance="float"] floating enable;
for_window [title="rofi"] floating enable;
# }}}
# Colors {{{

# client.background #fff000

# class                 border  backgr. text    indicator
client.focused          #f1ebeb #f1ebeb #acada1 #9358fe
client.focused_inactive #48483e #48483e #76715e #ae82ff
client.unfocused        #48483e #48483e #76715e #ae82ff
client.urgent           #fa2772 #fa2772 #acada1 #9358fe
# client.placeholder      #000000 #0c0c0c #ffffff #000000
# }}}
# Modes {{{
mode "resize" {
	bindsym h resize shrink width 10 px
	bindsym j resize grow height 10 px
	bindsym k resize shrink height 10 px
	bindsym l resize grow width 10 px
	# you can also use your mouse

	bindsym Return mode "default"
	bindsym Escape mode "default"
	bindsym g mode "gaps"
}

mode "gaps" {
	bindsym h gaps outer current plus 5
	bindsym j gaps inner current plus 5
	bindsym k gaps inner current minus 5
	bindsym l gaps outer current minus 5
	bindsym minus gaps inner current set 0
	bindsym equal gaps outer current set 0

	bindsym Shift+h gaps outer all plus 5
	bindsym Shift+j gaps inner all plus 5
	bindsym Shift+k gaps inner all minus 5
	bindsym Shift+l gaps outer all minus 5
	bindsym Shift+minus gaps inner all set 0
	bindsym Shift+equal gaps outer all set 0

	bindsym Return mode "default"
	bindsym Escape mode "default"
	bindsym r mode "resize"
}

mode "fl studio" {
	bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"
bindsym $mod+slash mode "fl studio"
# }}}
# Bar {{{

bindsym $mod+backslash bar hidden_state toggle zero
bindsym $mod+Shift+backslash bar mode toggle zero

# bar {
#         status_command i3status
# }

bar {
	i3bar_command i3bar -t
	status_command ~/bin/timeloop
	id zero

	font pango:Terminus 8
	
	# height 15

	mode hide
	hidden_state hide
	modifier Mod3

	position top
	tray_output primary
	workspace_buttons yes
	strip_workspace_numbers yes
	binding_mode_indicator yes



	colors {
			background #27282255
			statusline #f1ebebff
			separator #acada1
			# colorclass       border    backgr.   text
			focused_workspace  #272822dd #272822dd #cfd0c2ff
			active_workspace   #272822bb #272822bb #cfd0c2ff
			inactive_workspace #27282288 #27282288 #acada1ff
			urgent_workspace   #272822ff #cfd0c2ff #dc2566ff
			# urgent_workspace   #272822 #272822 #dc2566
		}
}
# }}}

# vim: fdm=marker syntax=i3
